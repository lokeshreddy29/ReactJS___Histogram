
# Objective
// when the user enters a word, React has to break it up into individual characters.
// the recurrence of each character / letter has to be stored in an array.
// the array of values should then be used to construct an alphabet histogram.